
extends ../disposicion/index


block contenido
    div.py-10
        h1.text-center.text-4xl.text-extrabold.my-10 Bienes
            span.font-normal Raices
        h2.text-center.text-2xl.text-extrabold= pagina
    div(class="bg-white shadow py-8 rounded-lg max-w-md mx-auto")
        p(class= error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-ful.font-bold.text-center.uppercase= mensaje

        if !error
        a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") Ya puedes iniciar sesión 